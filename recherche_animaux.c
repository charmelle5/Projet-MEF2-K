void Rechercher_animal(Animal* chenil1, int SIZE){
   if(chenil == NULL){
    printf("Erreur !");
    exit(1);
  }
char* nom; 
int espece;
printf("Taper le nom de l'animal :" \n)
scanf("%s,nom);
printf("Taper l'espèce de l'animal :" \n) 
scanf("%d,espece);
printf("Taper l'age de l'animal :" \n)
scanf("%d,nom);
for(int i=0; i<SIZE;i++){
   if( strcmp(nom)!=strcmp(chenil1[i].nom){
   }

